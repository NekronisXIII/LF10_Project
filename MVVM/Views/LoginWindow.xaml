<Window x:Class="LF10_Project.MVVM.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LF10_Project.MVVM.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Name"></Label>
        <TextBox Grid.Row="1" Text="{Binding Name}" Margin="5"></TextBox>
        <Label Grid.Row="2" Content="Passwort"></Label>
        <TextBox Text="{Binding Password}" Grid.Row="3" Margin="5"/>
        <Button IsDefault="True" Command="{Binding LoginCommand}" Grid.Row="4" Content="Login" Margin="5" Width="50" HorizontalAlignment="Right"></Button>
    </Grid>
</Window>
